---
import { getVersionedDocsPath } from '@libs/path'
import Code from '@shortcodes/Code.astro'
---

<section class="pb-medium-5 mb-xlarge">
  <div class="col-large-8 mb-xlarge">
    <div class="masthead-followup-icon d-inline-block mb-medium me-xsmall" style="--bg-rgb: var(--cx-danger-rgb);">
      <svg class="bi fs-1" aria-hidden="true"><use xlink:href="#list-square-solid"></use></svg>
    </div>
    <svg class="bi me-xsmall fs-2 fg-subtle" aria-hidden="true"><use xlink:href="#plus-solid"></use></svg>
    <div class="masthead-followup-icon d-inline-block mb-medium" style="--bg-rgb: var(--cx-info-rgb);">
      <svg class="bi fs-1" aria-hidden="true"><use xlink:href="#brackets-curly-solid-asterisk"></use></svg>
    </div>
    <h2 class="display-5 mb-medium fw-semibold lh-small">Components, meet the Utility&nbsp;API</h2>
    <p class="lead fw-normal">
      New in Chassis 5, our utilities are now generated by our <a href={getVersionedDocsPath('utilities/api')}
        >Utility API</a
      >. We built it as a feature-packed Sass map that can be quickly and easily customized. It's never been easier to
      add, remove, or modify any utility classes. Make utilities responsive, add pseudo-class variants, and give them
      custom names.
    </p>
  </div>
  <div class="row gx-medium-5">
    <div class="col-large-6">
      <h3 class="fw-semibold">Quickly customize components</h3>
      <p>
        Apply any of our included utility classes to our components to customize their appearance, like the navigation
        example below. There are hundreds of classes available—from <a href={getVersionedDocsPath('utilities/position')}
          >positioning</a
        > and <a href={getVersionedDocsPath('utilities/sizing')}>sizing</a> to <a
          href={getVersionedDocsPath('utilities/colors')}>colors</a
        > and <a href={getVersionedDocsPath('utilities/shadows')}>effects</a>. Mix them with CSS variable overrides for
        even more control.
      </p>
      <div class="p-large border rounded-3 mb-large">
        <ul class="nav nav-pills mb-large" id="pillNav" role="tablist">
          <li class="nav-item" role="presentation">
            <button
              class="nav-link active"
              id="home-tab"
              data-cx-toggle="tab"
              type="button"
              role="tab"
              aria-selected="true">Home</button
            >
          </li>
          <li class="nav-item" role="presentation">
            <button
              class="nav-link"
              id="profile-tab"
              data-cx-toggle="tab"
              type="button"
              role="tab"
              aria-selected="false">Profile</button
            >
          </li>
          <li class="nav-item" role="presentation">
            <button
              class="nav-link"
              id="contact-tab"
              data-cx-toggle="tab"
              type="button"
              role="tab"
              aria-selected="false">Contact</button
            >
          </li>
        </ul>
        <ul
          class="nav nav-pills nav-fill gap-xsmall p-2xsmall small bg-primary rounded-5 shadow-small"
          id="pillNav2"
          role="tablist"
          style="--cx-nav-link-color: rgba(255, 255, 255, .75); --cx-nav-link-hover-color: #fff; --cx-nav-pills-link-active-color: var(--cx-primary); --cx-nav-pills-link-active-bg: var(--cx-white);"
        >
          <li class="nav-item" role="presentation">
            <button
              class="nav-link active rounded-5"
              id="home-tab2"
              data-cx-toggle="tab"
              type="button"
              role="tab"
              aria-selected="true">Home</button
            >
          </li>
          <li class="nav-item" role="presentation">
            <button
              class="nav-link rounded-5"
              id="profile-tab2"
              data-cx-toggle="tab"
              type="button"
              role="tab"
              aria-selected="false">Profile</button
            >
          </li>
          <li class="nav-item" role="presentation">
            <button
              class="nav-link rounded-5"
              id="contact-tab2"
              data-cx-toggle="tab"
              type="button"
              role="tab"
              aria-selected="false">Contact</button
            >
          </li>
        </ul>
      </div>
      <Code
        code={`<ul class="nav nav-pills nav-fill gap-xsmall p-2xsmall small bg-primary rounded-5 shadow-small" id="pillNav2" role="tablist" style="--cx-nav-link-color: var(--cx-white); --cx-nav-pills-link-active-color: var(--cx-primary); --cx-nav-pills-link-active-bg: var(--cx-white);">
  <li class="nav-item" role="presentation">
    <button class="nav-link active rounded-5" id="home-tab2" data-cx-toggle="tab" type="button" role="tab" aria-selected="true">Home</button>
  </li>
  <li class="nav-item" role="presentation">
    <button class="nav-link rounded-5" id="profile-tab2" data-cx-toggle="tab" type="button" role="tab" aria-selected="false">Profile</button>
  </li>
  <li class="nav-item" role="presentation">
    <button class="nav-link rounded-5" id="contact-tab2" data-cx-toggle="tab" type="button" role="tab" aria-selected="false">Contact</button>
  </li>
</ul>`}
        lang="html"
      />
      <p class="d-flex mb-medium-0">
        <a href={getVersionedDocsPath('examples/#snippets')} class="icon-link icon-link-hover fw-semibold">
          Explore customized components
          <svg class="bi" aria-hidden="true"><use xlink:href="#arrow-right-long-solid"></use></svg>
        </a>
      </p>
    </div>
    <div class="col-large-6">
      <h3 class="fw-semibold">Create and extend utilities</h3>
      <p>
        Use Chassis's utility API to modify any of our included utilities or create your own custom utilities for any
        project. Import Chassis first, then use Sass map functions to modify, add, or remove utilities.
      </p>
      <Code
        code={`@import "chassis-css/scss/chassis";

$utilities: map-merge(
  $utilities,
  (
    "cursor": (
      property: cursor,
      class: cursor,
      responsive: true,
      values: auto pointer grab,
    )
  )
);`}
        lang="scss"
      />
      <p class="d-flex mb-medium-0">
        <a href={getVersionedDocsPath('utilities/api/')} class="icon-link icon-link-hover fw-semibold mb-medium">
          Explore the utility API
          <svg class="bi" aria-hidden="true"><use xlink:href="#arrow-right-long-solid"></use></svg>
        </a>
      </p>
    </div>
  </div>
</section>
