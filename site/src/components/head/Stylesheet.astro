---
import { getVersionedCxCssProps } from '@libs/chassis'
import { getVersionedDocsPath } from '@libs/path'
import type { Layout } from '@libs/layout'

interface Props {
  direction?: 'rtl'
  layout: Layout
}

const { direction } = Astro.props
---

<link {...getVersionedCxCssProps(direction)} />
<link rel="stylesheet" href={getVersionedDocsPath(`assets/icons/chassis-icons.min.css`)} />
