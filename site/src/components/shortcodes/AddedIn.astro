---
/*
 * Outputs badge to identify the first version something was added
 */

interface Props {
  version: string
}

const { version } = Astro.props
---
<span class="badge large success smooth mb-medium">Added in v{version}</span
>
